# Strategy definitions and their tunable parameters.
#
# Populated in Phase 5 (strategy implementations) and refined by Optuna in
# Phase 8. Left as an empty registry for now — no strategy exists yet, so
# there is nothing real to configure. Only parameters with a logical
# economic/trading justification belong here (see CLAUDE.md Section 14).

stop_loss_types: [atr, swing, fixed_pct, volatility]
take_profit_ratios: [1.0, 1.5, 2.0, 3.0]   # plus atr_target / trailing / break_even / partial, handled in code

strategies: {}
# Example shape to be filled in during Phase 5:
# strategies:
#   trend_breakout:
#     family: trend_following
#     enabled: false
#     symbols: [EURUSD, GBPUSD]
#     timeframes: [H1, H4]
#     parameters:
#       ema_period: 50
#       atr_period: 14
#       donchian_period: 20
